//next.config.mjs
/** @type {import('next').NextConfig} */
// next.config.mjs
const nextConfig = {
    output: "export",
    images: {
        unoptimized: true,
    },
    basePath: '/maq796113.github.io',
    trailingSlash: true
  };

export default nextConfig;
